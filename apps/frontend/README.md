# Agent1 Frontend

TypeScript + Vite operations dashboard scaffold.

## Local commands

- Install dependencies from monorepo root with `pnpm install`.
- Dev server: `pnpm --filter @agent1/frontend dev`
- Build: `pnpm --filter @agent1/frontend build`
