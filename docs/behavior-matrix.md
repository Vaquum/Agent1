# Behavior Matrix

Behavior scenarios and expected outcomes are defined in `spec.md`.

This file will track executable scenario IDs, assertions, and coverage status.

Current coverage includes self-trigger prevention for comment-driven events, bot-origin review-context filtering, deterministic PR author follow-up handling for review feedback and CI failures, Codex-backed remediation attempts for PR author follow-up workflows, runtime mode/scope enforcement with non-active zero-write behavior, startup fail-fast active-scope ownership fencing, deterministic ingress ordering persistence with per-entity high-water stale-event gating, deterministic entity persistence (`entities`) with normalized-event upsert/touch behavior and sandbox-scope tagging, lease-epoch mutating side-effect validation for stale-owner rejection, durable watcher-state persistence with stale reclaim and checkpoint restoration gates, explicit operator-required watcher escalation for irrecoverably stuck watcher rows, GitHub runtime safety policy enforcement for credential-owner preflight binding and default-deny capability checks, codex-runtime explicit git mutation allowlist/denylist blocking, codex-runtime per-environment branch namespace enforcement for explicit branch create/push commands with allowed/denied integration coverage, machine-readable permission-matrix schema coverage with required component/environment pair validation and persistence-role declarations, CI permission-matrix validation coverage via `tests/operations/permission_matrix_validation.py`, protected policy/guardrail mutation approval snapshot schema coverage with hash-locked file integrity checks and append-only audit-trail enforcement, CI protected mutation approval validation coverage via `tests/operations/protected_mutation_approval_validation.py`, CI workflow supply-chain coverage for immutable action pinning and per-job token-permission drift validation via `tests/operations/workflow_supply_chain_validation.py`, dependency vulnerability gate coverage for python and node with threshold/exception policy enforcement via `tests/operations/dependency_vulnerability_gate.py`, event-journal tamper-evident chain coverage for deterministic `event_seq`/`prev_event_hash`/`payload_hash` persistence and verification via `tests/operations/event_journal_chain_validation.py`, runtime rollout-policy schema validation with stage-to-health-signal integrity checks, rollout stage-gate evaluator pass/fail/missing-signal coverage for configured deployment stages, rollout guard rollback-trigger coverage for failed stage gates with deterministic active-to-shadow downgrade behavior, runtime stop-the-line threshold schema validation coverage with duplicate-rule prevention checks, runtime release-promotion precondition schema validation coverage with duplicate-precondition prevention checks, release-promotion gate service pass/fail precondition evaluation coverage and release-workflow gate wiring coverage, stop-the-line threshold breach evaluation coverage with deterministic automatic mode downgrade for active runtime, stop-the-line alert emission and operator acknowledgement event flow coverage with deterministic `alert_id` linkage, frontend Playwright coverage for dashboard shell navigation plus operator overview-filter, timeline drill-down, trace-pivot, selected-event detail inspection, and alert-anomaly feed rendering flows with CI browser setup baseline, deterministic scenario harness execution via `tests/scenarios/catalog.json`, deterministic direct-assignment, PR-mention, change-request follow-up, multi-round reviewer lifecycle-to-terminal-decision, and concurrent dev/prod sandbox-partition isolation scenario coverage in ingress coordinator and mention executor tests, minimal PR smoke set and environment contract definition via `tests/scenarios/pr-smoke-catalog.json` and `tests/scenarios/pr-smoke-env-contract.md`, PR-gate smoke wiring via `tests/scenarios/pr_smoke_run.py` with backend/frontend artifact upload coverage and documented fail/rerun procedure, nightly E2E browser artifact-retention and failure-summary coverage with timeout/schedule/concurrency guardrails, live GitHub sandbox smoke checks against `Vaquum/Agent1`, dashboard overview filtering/pagination on recent jobs/transitions/events, per-job dashboard timeline drill-down coverage including event detail inspection, reason-based transition correlation, trace pivot filtering, and action-attempt timeline query visibility, transactional outbox persistence coverage with dispatch lifecycle statuses (`pending`, `sent`, `confirmed`, `failed`, `aborted`), canonical idempotency key builder coverage for deterministic side-effect scope fields and schema components (`entity_key`, `action_type`, `target_identity`, `payload_hash`, `policy_version_hash`), outbox write-path schema enforcement and reconcile-path schema-component filtering (`idempotency_schema_version`, `idempotency_payload_hash`, `idempotency_policy_version_hash`), append-only `action_attempts` lifecycle persistence (`started`, `succeeded`, `failed`, `aborted`) with error metadata, durable `audit_runs` snapshot append/list persistence coverage with environment/status/type filtering and release-promotion gate execution wiring, deterministic `comment_targets` routing-target persistence from comment-routing execution paths with replay and idempotency lookup coverage, and retry reconciliation guard checks, runtime alert-signal emission and verification for lease violations, duplicate side-effect anomalies, comment-routing failures, outbox backlog growth, elevated failed-transition rates, hash-chain gap anomalies, idempotency-scope violations, and stop-the-line threshold breaches with required `trace_id`/`job_id`/runbook payload linkage, plus operational-readiness CI gating for required runbooks, readiness evidence artifacts, service-level/error-budget policy coverage, machine-readable alert-routing matrix integrity, incident lifecycle policy completeness, release-freeze control enforcement, and rollback-rehearsal evidence enforcement.
