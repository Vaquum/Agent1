# Behavior Matrix

Behavior scenarios and expected outcomes are defined in `spec.md`.

This file will track executable scenario IDs, assertions, and coverage status.

Current coverage includes self-trigger prevention for comment-driven events, bot-origin review-context filtering, deterministic PR author follow-up handling for review feedback and CI failures, Codex-backed remediation attempts for PR author follow-up workflows, runtime mode/scope enforcement with non-active zero-write behavior, startup fail-fast active-scope ownership fencing, deterministic ingress ordering persistence with per-entity high-water stale-event gating, deterministic entity persistence (`entities`) with normalized-event upsert/touch behavior and sandbox-scope tagging, lease-epoch mutating side-effect validation for stale-owner rejection, durable watcher-state persistence with stale reclaim and checkpoint restoration gates, explicit operator-required watcher escalation for irrecoverably stuck watcher rows, GitHub runtime safety policy enforcement for credential-owner preflight binding and default-deny capability checks, codex-runtime explicit git mutation allowlist/denylist blocking, policy-defined per-environment branch mutation namespace pattern scaffolding, deterministic scenario harness execution via `tests/scenarios/catalog.json`, live GitHub sandbox smoke checks against `Vaquum/Agent1`, dashboard overview filtering/pagination on recent jobs/transitions/events, per-job dashboard timeline drill-down coverage including event detail inspection, reason-based transition correlation, trace pivot filtering, and action-attempt timeline query visibility, transactional outbox persistence coverage with dispatch lifecycle statuses (`pending`, `sent`, `confirmed`, `failed`, `aborted`), canonical idempotency key builder coverage for deterministic side-effect scope fields and schema components (`entity_key`, `action_type`, `target_identity`, `payload_hash`, `policy_version_hash`), outbox write-path schema enforcement and reconcile-path schema-component filtering (`idempotency_schema_version`, `idempotency_payload_hash`, `idempotency_policy_version_hash`), append-only `action_attempts` lifecycle persistence (`started`, `succeeded`, `failed`, `aborted`) with error metadata, deterministic `comment_targets` routing-target persistence from comment-routing execution paths with replay and idempotency lookup coverage, and retry reconciliation guard checks, runtime alert-signal emission and verification for lease violations, duplicate side-effect anomalies, comment-routing failures, outbox backlog growth, and elevated failed-transition rates with required `trace_id`/`job_id`/runbook payload linkage, plus operational-readiness CI gating for required runbooks, readiness evidence artifacts, service-level/error-budget policy coverage, machine-readable alert-routing matrix integrity, incident lifecycle policy completeness, release-freeze control enforcement, and rollback-rehearsal evidence enforcement.
