# Policies

Store policy contracts that control safety, side effects, and allowed operations.

Policy controls include `agent_actor`, `ignored_actors`, and `ignored_actor_suffixes` for ingress self-trigger prevention.
